const snakeAndLadders= (playerRed, playerBlue)=> {

    const playerRed = playerRed; 
    const playerBlue = playerBlue

    const nextPlayer = {
        playerRed: playerBlue, 
        playerBlue: playerRed,
    }
}

module.exports = SnakeAndLadders; 